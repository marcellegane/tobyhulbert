const family = {
  brand: 'Helvetica Neue, Helvetica, Arial, sans-serif',
}

const weight = {
  normal: 400,
  medium: 500,
  bold: 700,
}

const size = {
  base: 16,
}

const Font = {
  family,
  weight,
  size,
}

export { Font }
